PORT=8000,
DB_USER=,
DB_HOST=,
DB_PASSWORD=,
DB_PORT=,
DB_NAME=,
# TOKEN_SECRET: Secret key for JWT signing. Must be a secure random string.
TOKEN_SECRET=
# AUTH_EXPIRY: JWT expiry time. Accepts values like '1d', '7d', '12h', or a number of seconds.
AUTH_EXPIRY=1d
NODE_ENV=,
BASE_URL=

