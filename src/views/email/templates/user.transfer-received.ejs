<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Transfer Received Notification</title>
    <style>
      body { font-family: Arial, sans-serif; background: #f9f9f9; color: #222; }
      .container { max-width: 420px; margin: 40px auto; background: #fff; padding: 24px; border-radius: 8px; box-shadow: 0 2px 8px #eee; }
      .amount { color: #28a745; font-weight: bold; font-size: 1.2em; }
      .footer { font-size: 0.9em; color: #888; margin-top: 24px; }
    </style>
  </head>
  <body>
    <div class="container">
      <h2>Hello <%= firstName %>,</h2>
      <p>You have received a transfer of <span class="amount"><%= amount %> <%= currency %></span> to your wallet <strong>#<%= toWalletId %></strong>.</p>
      <% if (fromName) { %>
        <p>Sender: <strong><%= fromName %></strong></p>
      <% } %>
      <% if (txId) { %>
        <p>Transaction ID: <strong><%= txId %></strong></p>
      <% } %>
      <div class="footer">
        Thank you for using Bukia HFT.
      </div>
    </div>
  </body>
</html>
