<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Transfer Status Notification</title>
    <style>
      body { font-family: Arial, sans-serif; background: #f9f9f9; color: #222; }
      .container { max-width: 420px; margin: 40px auto; background: #fff; padding: 24px; border-radius: 8px; box-shadow: 0 2px 8px #eee; }
      .status-success { color: #28a745; font-weight: bold; }
      .status-failed { color: #dc3545; font-weight: bold; }
      .footer { font-size: 0.9em; color: #888; margin-top: 24px; }
    </style>
  </head>
  <body>
    <div class="container">
      <h2>Transfer Status for <%= firstName %></h2>
      <p>
        Your transfer of <strong><%= amount %> <%= currency %></strong>
        from wallet <strong>#<%= fromWalletId %></strong>
        to wallet <strong>#<%= toWalletId %></strong>
        <span class="status-<%= status %>">
          was <%= status === "completed" ? "successful" : status === "failed" ? "unsuccessful" : status %>.
        </span>
      </p>
      <% if (txId) { %>
        <p>Transaction ID: <strong><%= txId %></strong></p>
      <% } %>
      <% if (reason && status === "failed") { %>
        <p><strong>Reason:</strong> <%= reason %></p>
      <% } %>
      <div class="footer">
        If you have any questions, please contact Bukia HFT support.
      </div>
    </div>
  </body>
</html>
